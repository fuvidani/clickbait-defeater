/**
 * Backend configuration
 */
const hostDomain = "davidmolnar.eu";
const hostPort = "10487";
const hostProtocol = "https";
const hostUrl = hostProtocol + "://" + hostDomain + ":" + hostPort;

/**
 * Enables logging within the extension
 *
 * @type {boolean}
 */
const logging = false;